name: Give Strava Kudos

on:
  # Triggers every 11:30am
  schedule:
    - cron: "0 1 * * *"
  workflow_dispatch:


jobs:
  run-kudos-cron:
    runs-on: ubuntu-20.04
    environment: STRAVA_EMAIL
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.7.6'
          cache: 'pip'
      - run: pip install -r requirements.txt
      - run: playwright install
      - run: python give_kudos_runnsw_club.py
      - run: python give_kudos_striders_club.py
      - run: python give_kudos_mrc_club.py
        env:
          STRAVA_EMAIL: ${{ secrets.STRAVA_EMAIL }}
          STRAVA_PASSWORD: ${{ secrets.STRAVA_PASSWORD }}
        
